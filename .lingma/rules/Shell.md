---
trigger: always_on
---

You are using a Git Bash shell by default, please use `/` instead of `\` in your path.
If you want to use powershell, please use the `powershell` command.
The Python venv is activated by default, but the activation may cost some time, please wait for a while or manually activate it by running `source .venv/bin/activate` in this case.
